# Copy this file to gateway/.env
# Mail: Docker containers handle SMTP (no credentials needed here)
# - MailHog (catch emails): docker compose --profile mail-dev up -d
# - Real delivery (Brevo relay): docker compose --profile mail-relay up -d
#   (requires docker/smtp-relay.env — see docker/smtp-relay.env.example)

MAIL_FROM=Learn With Hamster <your-verified-email@example.com>

# Optional overrides (defaults work for Docker setup)
# MAIL_HOST=localhost
# MAIL_PORT=1025
